{
    "name": "vialidades-monorepo",
    "version": "1.0.0",
    "description": "Monorepo for Vialidades App",
    "scripts": {
        "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
        "start": "cd server && npm start"
    }
}